Use to fix current commit without create new commit then you can change fix then use [[git amend]] to change and [[git push ]] again 
```
git add file_changes
git commit --amend 
git push --force 
```